# AP Microeconomics 重建 - 用户指示与问题记录

## 记录日期
2026-06-25

## 用户指示

> 先把所有的json都清空，确保没有污染，然后你把2012年的题目先逐题录入，录入完整的MCQ和FRQ、包括全部的题目、图表、特殊选项形式、FRQ的scoring rubric和里面带着的图像。只做2012年的，用你说的逐题录入的方式。然后再加上MCQ的题目分类、相似度组题。然后再设计一个全套的审计。做完之后我看看能做到什么程度。并且你要开始做到能够自我迭代，不能再浪费时间了。后面还有很多科目

**关键要求：**
1. 清空所有JSON（确保无旧数据污染）
2. 只做2012年
3. 逐题录入（非批量提取）
4. 包含：MCQ + FRQ + 图表 + 特殊选项 + Scoring Rubric + 图像
5. 完成后：MCQ分类 + 相似度组题
6. 设计全套审计
7. 用户验收后才能继续

---

## 历史问题清单（已确认）

### 机制层面问题
- **提取脚本不可靠**：`get_text()` 返回顺序可能错乱，双栏布局处理失败
- **审计机制缺失**：没有逐题内容验证，只检查结构存在性
- **Pipeline执行不完整**：跳过了 Phase 4d 前端渲染审计和 Phase 9 全量审计
- **问题分级被降级**：把 `image_missing` 从 P0 降级为 warning，让问题通过
- **没有版本控制**：每次修复都是覆盖，无法追溯

### 数据层面问题（当前JSON）
- **419/419题缺少 `answer` 字段**（P0致命）
- **16个选项为"See graph"占位符**（P0致命）
- **65题有页面污染**（P0致命）
- **图片截断问题**（图像底部被裁剪）
- **3题文本可能截断**
- **25题PDF交叉验证失败**

### 执行层面问题
- **没有逐题确认**：批量提取后没有单题验证
- **没有与PDF原文对比**：不知道提取的是否正确
- **用户多次提醒后仍未改进**：重复同样的错误模式

---

## 2012年重建范围

### 必须包含的内容
- [ ] 60道MCQ（完整文本 + 选项 + 答案 + 图表/特殊选项）
- [ ] 3道FRQ（完整文本 + 评分标准 + 图像）
- [ ] 所有图形题图片（完整不截断）
- [ ] 所有表格题结构化数据
- [ ] 所有博弈论矩阵数据
- [ ] 单元分类（U1-U6）
- [ ] 相似度组题索引

### 交付物
- `public/data/ap/microeconomics/2012_question_bank.json`（MCQ）
- `public/data/ap/microeconomics/2012_frq_bank.json`（FRQ）
- `public/images/ap/microeconomics/2012_*.png`（图表）
- `public/data/ap/microeconomics/2012_similarity_index.json`（相似度）
- `docs/2012_AUDIT_REPORT.md`（审计报告）

---

## 执行步骤

1. 清空现有JSON（备份到 `backup/`）
2. 逐题录入2012年MCQ（人工从PDF复制）
3. 逐题录入2012年FRQ（人工从PDF复制）
4. 裁剪所有图表/图像
5. 单元分类
6. 相似度组题
7. 设计全套审计
8. 用户验收

**状态：等待执行**
